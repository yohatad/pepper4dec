# Core scientific computing
numpy==2.2.6
scipy==1.15.3

# ONNX runtime for Silero VAD
onnxruntime==1.23.2
onnxruntime-gpu==1.23.2  # For GPU acceleration

# Speech recognition
faster-whisper==1.2.1

# Sound localization (SRP-PHAT beamforming)
pyroomacoustics==0.7.3

# PyTorch is deliberately NOT required. The Silero VAD runs on onnxruntime and
# faster-whisper runs on CTranslate2 -- both native runtimes. torch was
# previously imported only to hold the VAD's recurrent state as tensors (numpy
# does that) and to print the CUDA device name in a single log line.